fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
geoalchemy2==0.15.1
shapely==2.1.2
pydantic==2.13.4
python-dotenv==1.0.1
requests==2.34.2
# auth/multi-tenancy (added with feature/multi-tenancy): password hashing,
# JWT signing, and EmailStr validation (pydantic pulls email-validator in
# only when EmailStr is used, so it must be listed explicitly).
bcrypt==5.0.0
PyJWT==2.13.0
email-validator==2.3.0
